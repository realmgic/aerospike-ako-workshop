# Lab 2.4: upgrade to 8.1.2.x — overlay for helm/base-dim-cluster-values.yaml or helm/base-disk-cluster-values.yaml
image:
  repository: aerospike/aerospike-server-enterprise
  tag: "8.1.2.0"
aerospikeConfig:
  service:
    cgroup-mem-tracking: true
